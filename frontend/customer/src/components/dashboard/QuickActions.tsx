import { Activity, ArrowDownToLine, ArrowUpRight, Landmark } from "lucide-react";
import { Link } from "react-router-dom";
import { ROUTES } from "@/routes/paths";
type Props={disabled?:boolean};
const actions=[{label:"Send money",shortLabel:"Send",to:ROUTES.transfer,icon:ArrowUpRight,primary:true},{label:"Deposit",shortLabel:"Deposit",to:ROUTES.deposit,icon:Landmark},{label:"Withdraw",shortLabel:"Withdraw",to:ROUTES.withdraw,icon:ArrowDownToLine},{label:"Activity",shortLabel:"Activity",to:ROUTES.activity,icon:Activity}];
export default function QuickActions({disabled=false}:Props){return <div className="grid grid-cols-4 gap-2 lg:flex lg:flex-wrap">{actions.map(({label,shortLabel,to,icon:Icon,primary})=>disabled?<button key={label} type="button" disabled className="flex min-h-16 items-center justify-center gap-2 rounded-[var(--radius-control)] border px-2 text-xs font-medium opacity-45 sm:min-h-12 sm:px-3 sm:text-sm" style={{color:"var(--muted)",background:"var(--surface)",borderColor:"var(--border)"}}><Icon size={17}/><span className="hidden sm:inline">{label}</span><span className="sm:hidden">{shortLabel}</span></button>:<Link key={label} to={to} className="flex min-h-16 items-center justify-center gap-2 rounded-[var(--radius-control)] border px-2 text-xs font-medium transition hover:-translate-y-px sm:min-h-12 sm:px-3 sm:text-sm lg:min-w-32" style={{color:primary?"#fff":"var(--text)",background:primary?"var(--brand-primary)":"var(--surface)",borderColor:primary?"var(--brand-primary)":"var(--border)"}}><Icon size={17}/><span className="hidden sm:inline">{label}</span><span className="sm:hidden">{shortLabel}</span></Link>)}</div>}
